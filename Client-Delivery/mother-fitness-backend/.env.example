# Server Configuration
PORT=5000
NODE_ENV=production

# Database Configuration (MongoDB Atlas)
MONGODB_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/mother_fitness_db?retryWrites=true&w=majority

# Email Service (Gmail via Nodemailer)
EMAIL_USER=your_email@gmail.com
EMAIL_PASSWORD=your_app_specific_password

# Admin Credentials (for initial setup if needed, though usually in DB)
ADMIN_EMAIL=admin@motherfitness.com
